name: Legacy Instagram processor (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Instagram processing moved to the local Windows application."
          echo "This workflow is intentionally disabled and does not download anything."
